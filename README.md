# 8-puzzle

todo :
- ~~display running time~~ **DONE** 
- ~~display search depth~~ **DONE**
- ~~display explored nodes~~ **DONE** 
- implement check solvable
- no. of steps -= 1?
- report

extras: #GUI 
- colorize the search tree
- implement manual mode
- clear analysis history independtly

GUI run note:
- Please install the font provided in 'assets/fonts/joystix.zip' for better visualization
- If the search tree nodes are stacked above each other horizontally, restart the search (start button) 3shan el tree is generated randomly each run time so I can't control the horizontal padding dynamically
